# ============================================================
# SERVER ENVIRONMENT CONFIGURATION
# ============================================================

PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# MongoDB Atlas Connection URI
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.owyzur7.mongodb.net/portfolio_db?retryWrites=true&w=majority

# ============================================================
# GMAIL SMTP CONFIGURATION (Nodemailer)
# ============================================================
# Preferred: port 465 with SSL/TLS
# Generate a 16-character App Password at:
# Google Account -> Security -> 2-Step Verification -> App Passwords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASS=
CONTACT_TO=akileshanand302006@gmail.com
